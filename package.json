{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev" : "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.1.0",
    "turbo": "^1.10.16",
    "vite": "^4.5.0"
  }
}
